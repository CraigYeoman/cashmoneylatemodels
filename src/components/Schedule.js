import schedule from "../images/schedule.jpg"

const Schedule = () => {

    return (
      <div className="container">
        <img src={schedule} className="w-100 pt-3" alt="schedule"/>
      </div>
    );
  };
  
  export default Schedule;